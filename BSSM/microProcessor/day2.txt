2022 03 22 2112 임시온

2. 작성할 내용
1) 수업 내용
1-1) 리눅스 사용 이유
1. 돈이 안들어서 좋음
2. 많은 하드웨어를 지원함
3. 리눅스의 일관된 인터페이스
4. 생산성을 높이는 Command-Line Interface
생산성이 좋은 이유 )
리눅스의 명령어로 한번에 다 파일을 만들 수 있음
딴 os 는 전부 일일이 하나하나 만들어서 해야하는데 리눅스는 명령어로 동시에 여러개가 가능하니까 좋음

1-2) toolchain & cross tool chain
toolchain : 프로그램(실행파일) 개발을 하는데 필요한 개발 도구들의 모임
cross-toolchain : 컴파일 해서 만든 실행코드가 컴파일한 os 가 아닌 다른 os에서 실행코드를 생성하는 컴파일러를 말함
* main에서 return 0 은 OS가 받음 in C langauge

ARM(회사) 이 만든 cpu cortxt - M(micro Controler)(os가 거의 안올라감) , R(Real time) , A(Application)
touch 갱신 , 파일 생성1. OS 가 다를 때

other 자리에 scanf를 통해 피연산자 2개와 연산자를 받아서 int calc(int opd1,char op, in opd2)%i %c %i
2) 심볼
mean : 컴파일 과정에서 아직 처리되지 않은 전역변수 & 함수 이름을 말함
-> 나중에 심볼이 메모리 주소로 변하게 되는데 이게 처리 되면 심볼 처리 됨 아니면 심볼 처리 안됬다고 말함
이유 : C 컴파일을 할 때 하나씩 하는데 다른 파일에 함수가 정의되어있을 수도 있어서 심볼로 미리 남겨놓는 거임 주소를 처리 하지 않고

2-1)
main return 0 을 os가 받는데 MPU 는 os 가 들어갈 수 없을 정도로 작음(?) 그런 경우에는 인터럽트라고 하는 개념이 들어감
인터럽트(interrupt) = (입출력 하드웨어에서)예외 상황이 발생하여 처리 가능하도록 알려주는 것을 말함

2) 기억에 남는 내용
왜 그렇게 하는지 어떻게 작동하는지 어떤 마음 가짐을 가져야 하는지 그런 것을 왜 그렇게 사용하는지를 알 필요가 있다.
Q 왜 개발할 때 리눅스를 사용하는가? 같은 사소한 것에도 이유를 알아야함
2-1) 무엇을 해야 하나
(신문)기사 읽기 프로그래머들이 쓴 기사들을(세상을 많이 알 필요가 있다)
코드 리뷰? 논문 신문 기사 등등 세상에 좀 관심을 가지기 내 분야에 관한 것에 대한 관심을 가지기

참고 할 것들)
* 포트 포워딩으로 해서 열어 주지 않는 이상은 외부에서는 내 리눅스로 접근 못함
* Router(라우터) - 컴퓨터 네트워크 간에 데이터를 경로에 따라 전송해주는 장치
* 윈도우가 라우터(Router) 역할을 해서 리눅스 아이피를 계속 바꿈(LINUX IP) 를 계속 바꿈
  -> 계속 접속 할 때 마다 ip 확인해서 ssh connect 해줘야됨
* ssh 는 암호화 된 접속임(I/O 을 암호화 해서 통신하기에 나중에 암호화 키를 저장해라고 하면 yes 하면됨) 됨
* vm ware = Application 가상화라는 개념 , wsl = cunurl 가상화 개념 
* speed VM WARE < wsl 나중에 vm ware 는 거의 안씀
* gdb는 debuging 해주는 프로그램
* gcc(c 컴파일러) 를 tool-chain 이라고 말함
* host-tool chain 과 cross tool chain을 구분을 할 수 있어야 함
* host tool chain 은 모든 사람이 쓰지만 cross tool chain 은 전문가만 씀 ㅇㅇ
* Toolchain - You need make computer program 개발 도구 모음을 뜻함
* cross tool chain -> host computer 말고 다른 os 에서 실행가능한 거
* extern - 외부에 정의 되어있다는 거임
느낀점
너무 새로운 용어가 많이 등장해서 이해하기 힘들어서 내가 좀 많이 부족하다고 생각하게 되었다.
평소에도 거의 요즘들어서 내가 진짜 취업을 할 수 있을까 라고 생각을 많이 했었는데 강의를 듣고 나니까 취업은 거의 무조건
할 수 있겠다라고 생각을 하게 되었다. 취업은 할 수 있지만 돈의 액수가 결정이 된다는 것을 깨닫고 솔직히 맘이 조금 편안해졌다.
나 스스로 많이 부족하다고 느꼈다.